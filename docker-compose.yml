version: '3'

services:
  php:
    build:
      context: ./backend
      dockerfile: Dockerfile
    volumes:
      - ./backend/php/php.ini:/etc/php/8.2/cli/php.ini
      - .:/workspace/server
    restart: on-failure:5
  nginx:
    depends_on:
      -  php
    image: nginx:latest
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx:/etc/nginx/conf.d
      - .:/workspace
    restart: on-failure:5